# Encore secrets (required for deploy) — encore secret set --type prod,dev <Name>
# BinanceAPIKey
# BinanceAPISecret
# BinanceUseTestnet        → false
# AegisTradingEnabled      → false
# AegisEnv                 → production

# Next.js dashboard (dashboard/.env.local)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
